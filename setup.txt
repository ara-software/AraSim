EXPONENT=21

//the neutrinos
NNU=300 //allow this many neutrinos
ONLY_PASSED_EVENTS=0 //throw only NNU events
EVENT_GENERATION_MODE=0 //read in an event list

//the askaryan signal and interactions
SIMULATION_MODE=1 //time domain askaryan radiation
SECONDARIES=0 //no secondary interactions
taumodes=0 //no fancy tau decays

//the detector
DETECTOR=1 //1 station
BORE_HOLE_ANTENNA_LAYOUT=11 //design a "strawman + PA"
//11 = design a "strawman + PA"
//12 = "punch" at 100m
//13 = "punch" at 60m

//the electronics and noise
NFOUR=1024 // NFOUR/2 is number of samps in time domain
NOISE_WAVEFORM_GENERATE_MODE=0 // new noise waveforms for each event
NOISE_EVENTS=90 // number of pure noise waveforms
DATA_BIN_SIZE=4096 //length of pregenerated noise waveforms
V_MIMIC_MODE=0 // 0 : global trig is in center of readout windows
TIMESTEP=8.333E-10 //1.2 GHz sampling rate
TRIG_WINDOW=50e-9 //mult-channel coincidence window in ns
CUSTOM_ELECTRONICS=1 //yes custom electronics

//the trigger
TRIG_ANALYSIS_MODE=2 //0: S+N, 1: S only, 2: N only
TRIG_SCAN_MODE=9 //use hi+lo coincidence with LPDA, PA etc.

//the ice and ray tracing
NOFZ=1 //use ice with n(z)
POSNU_RADIUS=7000 // radius of the cylinder the event is thrown in
RAYSOL_RANGE=9000 // radius of the cylinder in ray tracer will look for solutions

//the output
DATA_SAVE_MODE=1 // 1: save only final waveforms
DATA_LIKE_OUTPUT=0 //don't try to save anything to event tree
ANTENNA_MODE=0
LPDA_MODE=1

NOISE_WAVEFORM_GENERATE_MODE=0 // generates new waveforms for every event
NOISE=0 //flat thermal noise
NOISE_CHANNEL_MODE=0 //using different noise temperature for each channel
NOISE_TEMP=300
PHASING_MODE=1 //Phased array decreases noise by sqrt(8)
